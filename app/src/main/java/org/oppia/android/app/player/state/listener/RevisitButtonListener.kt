package org.oppia.android.app.player.state.listener

/** Listener for when the 'Revisit Previous Question' button is clicked. */
interface RevisitButtonListener {
  fun onRevisitButtonClicked(stateName: String)
}
