package org.oppia.android.app.devoptions

/** Listener for when the user wants to create one profile. */
interface AddOneProfileButtonClickListener {
  /** Called when the user indicates that they want to create one profile. */
  fun createOneProfile()
}
